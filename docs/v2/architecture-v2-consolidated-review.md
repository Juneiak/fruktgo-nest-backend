# Architecture V2 — Консолидированный анализ предложений

> Объединение предложений из `architecture-v2-improvements.md` и `architecture-critique-roadmap.md`.
> Для каждого пункта: актуальность, сложность внедрения, важность, решение и вердикт (внедрять сейчас / отложить / отклонить).

---

## Сводная таблица

| # | Тема | Важность | Сложность | Актуальность для MVP | Вердикт |
|---|------|----------|-----------|----------------------|---------|
| 1 | EventEmitter2 → BullMQ | 🔴 BLOCKER | Medium | ✅ Критично | **Внедрить в Фазе 0** |
| 2 | MongoDB OCC вместо транзакций | 🔥 High | Low | ✅ Критично | **Внедрить в Фазе 1** |
| 3 | Multi-tenant / city инварианты | 🔥 High | Low | ✅ Критично | **Внедрить в Фазе 1** |
| 4 | Idempotency keys (Payments) | 🔥 High | Medium | ✅ Критично | **Внедрить в Фазе 2** |
| 5 | Buy > Build (LOYALTY, SUPPORT) | 💰 High | Low | ✅ Критично | **Упростить в MVP** |
| 6 | Security / PII базовый | 🔥 High | Medium | ✅ Критично | **Внедрить в Фазе 1** |
| 7 | FSM для оркестраторов | 🔸 Medium | Low | ⚠️ Желательно | **Внедрить в Фазе 1** |
| 8 | Observability / SLO базовый | 🔸 Medium | Medium | ⚠️ Желательно | **Внедрить в Фазе 2** |
| 9 | BFF / Aggregate Endpoints | 🔸 Medium | Low | ⚠️ Желательно | **Внедрить в Фазе 2** |
| 10 | Feature flags | 🔸 Medium | Low | ⚠️ Желательно | **Внедрить в Фазе 2** |
| 11 | Anti-abuse / Promo лимиты | 🔸 Medium | Low | ⚠️ Желательно | **Внедрить в Фазе 2** |
| 12 | Logistics SLA / Fallback | 🔸 Medium | Medium | ⏳ Можно позже | **Отложить на Фазу 3** |
| 13 | Search / ES синк SLA | 🔸 Medium | High | ⏳ Можно позже | **Отложить на Фазу 3** |
| 14 | Data lifecycle / Retention | ⚪ Low | Medium | ⏳ Можно позже | **Отложить на Фазу 3+** |
| 15 | Cost / Cache стратегия | ⚪ Low | Medium | ⏳ Можно позже | **Отложить на Фазу 3+** |
| 16 | Governance / RACI | ⚪ Low | Low | ⏳ Можно позже | **Отложить на Фазу 3+** |
| 17 | Outbox/Saga полноценный | 🔸 Medium | High | ⏳ Overengineering | **Отложить** |
| 18 | OpenTelemetry полный | 🔸 Medium | High | ⏳ Overengineering | **Отложить** |

---

## Детальный анализ

### 1. EventEmitter2 → BullMQ (BLOCKER)

**Источник:** architecture-critique-roadmap.md, Риск #1

**Проблема:** EventEmitter2 работает только внутри одного процесса. При >1 инстансе события теряются.

**Обоснование:** Это фундаментальная проблема. Без распределённого брокера нельзя масштабировать горизонтально. События order.created, payment.completed могут потеряться при падении процесса.

**Решение:**
```typescript
// src/infra/event-bus/event-bus.port.ts
export interface EventBusPort {
  emit(eventName: string, payload: any): Promise<void>;
  subscribe(eventName: string, handler: (payload: any) => Promise<void>): void;
}

// BullMQ реализация для прода, EventEmitter для локальной разработки
```

**Сложность:** Medium (NestJS имеет готовый @nestjs/bull модуль)

**Вердикт:** ✅ **ВНЕДРИТЬ В ФАЗУ 0** — без этого нельзя в прод.

---

### 2. MongoDB OCC вместо тяжёлых транзакций

**Источник:** architecture-critique-roadmap.md, Риск #2

**Проблема:** Транзакции MongoDB тяжёлые, блокируют документы. INVENTORY — самый нагруженный модуль.

**Обоснование:** Для MVP достаточно Optimistic Concurrency Control (OCC) через поле `version`. Это проще и быстрее.

**Решение:**
```typescript
// Атомарное списание с проверкой версии
const result = await stockModel.updateOne(
  { _id: stockId, quantity: { $gte: amount }, __v: currentVersion },
  { $inc: { quantity: -amount }, $set: { __v: currentVersion + 1 } }
);
if (!result.modifiedCount) throw new ConflictException('Retry');
```

**Сложность:** Low (изменение паттерна записи)

**Вердикт:** ✅ **ВНЕДРИТЬ В ФАЗУ 1** — критично для производительности INVENTORY.

---

### 3. Multi-tenant / city инварианты

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет явных инвариантов city/tenantId. Риск пересечения данных между городами.

**Обоснование:** Платформа работает в нескольких городах. Без явного city в ключевых моделях возможны баги (клиент видит магазины другого города).

**Решение:**
- Добавить `city: string` в Order, ShopProduct, Stock, Customer.address
- Индексы: `{ city: 1, ... }`
- Фильтрация на уровне портов (все запросы обязаны передавать city)

**Сложность:** Low (добавление поля + индексы)

**Вердикт:** ✅ **ВНЕДРИТЬ В ФАЗУ 1** — критично для корректности данных.

---

### 4. Idempotency keys для Payments

**Источник:** architecture-v2-improvements.md + architecture-critique-roadmap.md

**Проблема:** Без idempotency возможны дубликаты платежей/возвратов.

**Обоснование:** Платёжные провайдеры (ЮKassa) поддерживают idempotency key. Это обязательно для финансовой целостности.

**Решение:**
```typescript
// При создании платежа
const idempotencyKey = `order:${orderId}:payment:${attempt}`;
await yookassa.createPayment({ ... }, { idempotencyKey });
```

**Сложность:** Medium (нужно хранить и проверять ключи)

**Вердикт:** ✅ **ВНЕДРИТЬ В ФАЗУ 2** — обязательно до запуска платежей.

---

### 5. Buy > Build (LOYALTY, SUPPORT)

**Источник:** architecture-critique-roadmap.md, Риск #4

**Проблема:** Самописная лояльность и тикетница — это месяцы разработки.

**Обоснование:** На старте это не конкурентное преимущество. Лучше потратить время на core функционал.

**Решение:**
- **LOYALTY:** Упростить до `bonusBalance: number` в Customer. Никаких уровней и сложных правил.
- **SUPPORT:** Использовать Telegram для чата + простая сущность `Issue` для связки. Не писать свою тикетницу.

**Сложность:** Low (упрощение, а не усложнение)

**Вердикт:** ✅ **УПРОСТИТЬ В MVP** — экономия 2-3 месяцев разработки.

---

### 6. Security / PII базовый

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет политики хранения PII, rate limiting.

**Обоснование:** Телефоны, адреса, документы селлеров — это персональные данные. Нужна базовая защита.

**Решение (MVP):**
- Rate limiting на auth endpoints (10 req/min per IP)
- Не логировать полные телефоны/адреса
- Хранить платёжные токены, а не данные карт (через провайдера)

**Сложность:** Medium

**Вердикт:** ✅ **ВНЕДРИТЬ В ФАЗУ 1** — базовый уровень обязателен.

---

### 7. FSM для оркестраторов

**Источник:** architecture-critique-roadmap.md, Риск #3

**Проблема:** `if/else` в оркестраторах превращается в спагетти.

**Обоснование:** Явная машина состояний упрощает тестирование и поддержку.

**Решение:**
```typescript
const OrderTransitions = {
  [OrderStatus.CREATED]: [OrderStatus.PAID, OrderStatus.CANCELLED],
  [OrderStatus.PAID]: [OrderStatus.ASSEMBLING, OrderStatus.REFUNDED],
  // ...
};

function canTransition(from: OrderStatus, to: OrderStatus): boolean {
  return OrderTransitions[from]?.includes(to) ?? false;
}
```

**Сложность:** Low (простой map, без xstate на старте)

**Вердикт:** ✅ **ВНЕДРИТЬ В ФАЗУ 1** — упрощает код оркестраторов.

---

### 8. Observability / SLO базовый

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет метрик, трассировки, алертов.

**Обоснование:** Без observability сложно диагностировать проблемы в проде.

**Решение (MVP):**
- Логирование ключевых операций (checkout, payment)
- Базовые метрики (request count, latency) через Prometheus
- Алерты на ошибки 5xx

**Сложность:** Medium

**Вердикт:** ⚠️ **ВНЕДРИТЬ В ФАЗУ 2** — важно, но не блокер для MVP.

---

### 9. BFF / Aggregate Endpoints

**Источник:** architecture-critique-roadmap.md, Риск #5

**Проблема:** Мобильному приложению придётся делать много запросов для одного экрана.

**Обоснование:** Влияет на UX, особенно на медленных сетях.

**Решение:**
- Создать aggregate endpoints для ключевых экранов
- `GET /customer/orders/:id/details` — полный агрегат заказа

**Сложность:** Low

**Вердикт:** ⚠️ **ВНЕДРИТЬ В ФАЗУ 2** — улучшает UX, но не критично для MVP.

---

### 10. Feature flags

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет механизма поэтапного включения функций.

**Обоснование:** Позволяет безопасно релизить новые фичи.

**Решение:**
- Простой конфиг в БД или env: `FEATURE_NEW_CHECKOUT=true`
- Проверка в RoleService перед выполнением

**Сложность:** Low

**Вердикт:** ⚠️ **ВНЕДРИТЬ В ФАЗУ 2** — полезно, но не блокер.

---

### 11. Anti-abuse / Promo лимиты

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет защиты от злоупотреблений промокодами.

**Обоснование:** Можно потерять деньги на абьюзерах.

**Решение (MVP):**
- Лимит использования промокода на клиента
- Лимит промокодов в день на IP
- Антиспам в отзывах (1 отзыв на заказ)

**Сложность:** Low

**Вердикт:** ⚠️ **ВНЕДРИТЬ В ФАЗУ 2** — важно до маркетинговых кампаний.

---

### 12. Logistics SLA / Fallback

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет целевых SLA, нет fallback-провайдеров.

**Обоснование:** Важно для качества, но на старте будет один провайдер.

**Решение:** Определить SLA позже, когда будет статистика.

**Сложность:** Medium

**Вердикт:** ⏳ **ОТЛОЖИТЬ НА ФАЗУ 3** — на MVP один провайдер, SLA определим по факту.

---

### 13. Search / ES синк SLA

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет стратегии синхронизации INVENTORY → ElasticSearch.

**Обоснование:** Важно для масштабирования, но на MVP достаточно MongoDB $text search или Atlas Search.

**Решение:** ES подключить в Фазе 3, когда будет нагрузка.

**Сложность:** High

**Вердикт:** ⏳ **ОТЛОЖИТЬ НА ФАЗУ 3** — premature optimization для MVP.

---

### 14. Data lifecycle / Retention

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет политики ретенции логов/событий.

**Обоснование:** Важно для compliance и cost, но не критично на старте.

**Решение:** Определить TTL для коллекций AUDIT/ANALYTICS позже.

**Сложность:** Medium

**Вердикт:** ⏳ **ОТЛОЖИТЬ НА ФАЗУ 3+** — можно решить когда вырастут объёмы.

---

### 15. Cost / Cache стратегия

**Источник:** architecture-v2-improvements.md

**Проблема:** Нет стратегии кэширования.

**Обоснование:** Оптимизация производительности, но на MVP нагрузка небольшая.

**Решение:** Redis кэш для STOREFRONT в Фазе 3.

**Сложность:** Medium

**Вердикт:** ⏳ **ОТЛОЖИТЬ НА ФАЗУ 3+** — premature optimization.

---

### 16. Governance / RACI

**Источник:** architecture-v2-improvements.md

**Проблема:** Не описаны роли и права на ручные операции.

**Обоснование:** Важно для масштабирования команды, но на старте команда маленькая.

**Решение:** Описать RACI когда команда вырастет.

**Сложность:** Low (документация)

**Вердикт:** ⏳ **ОТЛОЖИТЬ НА ФАЗУ 3+** — не критично для маленькой команды.

---

### 17. Outbox/Saga полноценный

**Источник:** architecture-v2-improvements.md

**Проблема:** Нужен Outbox pattern для гарантированной доставки событий.

**Обоснование:** Важно для масштабирования, но на MVP BullMQ с retry достаточно.

**Решение:** BullMQ покрывает 90% кейсов. Полноценный Outbox — когда будет реальная проблема.

**Сложность:** High

**Вердикт:** ⏳ **ОТЛОЖИТЬ** — overengineering для MVP. BullMQ достаточно.

---

### 18. OpenTelemetry полный

**Источник:** architecture-v2-improvements.md

**Проблема:** Нужна полная трассировка.

**Обоснование:** Важно для диагностики, но полный OTel — это сложно.

**Решение:** Начать с базового логирования + Prometheus метрик. OTel в Фазе 3.

**Сложность:** High

**Вердикт:** ⏳ **ОТЛОЖИТЬ** — базовых метрик достаточно для MVP.

---

## Чек-лист "Не делай глупостей" (из critique-roadmap)

Эти правила **ОБЯЗАТЕЛЬНЫ** с первого дня:

- [x] **Не пиши свой чат.** Telegram SDK для уведомлений.
- [x] **Не храни картинки в Base64.** S3/R2 сразу.
- [x] **Не используй float для денег.** Только Int (копейки) или Decimal128.
- [x] **Все даты в UTC.** Таймзона магазина отдельным полем.
- [x] **Не делай Distributed Transactions.** Saga или Eventual Consistency.

---

## Итоговый Roadmap

### Фаза 0: Фундамент (до кода)
- [x] Setup Redis + BullMQ модуль
- [x] ESLint правила для изоляции модулей
- [x] EventBusPort абстракция

### Фаза 1: MVP Core
- [x] AUTH + CUSTOMER + CATALOG + BUSINESS
- [x] ORDERS (простой) + INVENTORY (OCC)
- [x] city/tenantId инварианты
- [x] FSM для статусов заказа
- [x] Базовый Security (rate limiting, no PII in logs)
- [x] LOYALTY упрощённый (bonusBalance)
- [x] SUPPORT через Telegram (без своей тикетницы)

### Фаза 2: Money Flow
- [x] FINANCE + Idempotency keys
- [x] LOGISTICS (один провайдер)
- [x] Базовый Observability (Prometheus)
- [x] Feature flags
- [x] Anti-abuse лимиты
- [x] BFF aggregate endpoints

### Фаза 3: Scale
- [ ] Redis Cache для STOREFRONT
- [ ] ElasticSearch для поиска
- [ ] Analytics (ClickHouse)
- [ ] Logistics SLA + fallback
- [ ] Data retention policies
- [ ] OpenTelemetry полный

---

## Что НЕ внедрять (Overengineering для MVP)

| Предложение | Причина отклонения |
|-------------|-------------------|
| Полноценный Outbox/Inbox | BullMQ с retry достаточно |
| OpenTelemetry полный | Базовых метрик хватит |
| Своя тикетница | Telegram дешевле |
| Сложная лояльность | bonusBalance хватит |
| ES сразу | MongoDB Atlas Search хватит |
| Redis для INVENTORY сразу | OCC хватит для MVP нагрузки |
| RACI матрица | Маленькая команда |
