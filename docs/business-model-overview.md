# Business Model (draft v0.1)

Документ описывает **предполагаемую бизнес-модель платформы** на уровне, достаточном для:
- проектирования backend-доменов;
- понимания связей сущностей и инвариантов;
- использования ИИ как контекста («как всё устроено с бизнес-стороны»).

Детальные процессы выделены в отдельные документы:
- **Процессы по типам:** @/docs/business/processes/ (заказ, регистрация, оплата и т.д.)

Документ — живой, по мере уточнения бизнес-модели дополняется и правится.

---

## 1. Общая концепция продукта

Платформа — это **маркетплейс относительно быстрой доставки**:

- фрукты, овощи, ягоды;
- зелень;
- орехи, сухофрукты;
- сопутствующая бакалея (крупы, ореховые пасты и т.п.).

Форматы клиентских интерфейсов:
- мобильное приложение;
- веб-сайт;
- (дополнительно) Telegram-бот.

Основная идея для клиента:
- быстро, удобно, «как Xiaomi в мире еды» — без излишеств, но стабильно, предсказуемо и качественно;
- акцент на свежесть, минимальную боль при выборе и оплате;
- **гарантия качества продуктов** (аналог китайского Poizon для продуктов):
  - если товар не соответствует заявленному качеству (лежалый, тухлый, недовес, повреждения), клиент может открыть **спор** между собой и магазином;
  - платформа выступает арбитром и **гарантом качества**, при этом больше на стороне клиента для обеспечения доверия;
  - возможные решения спора: возврат денег, предоставление скидки, замена товара;
  - каждая посылка проверяется на качество (визуально + вес);
  - ведётся **рейтинг надёжности клиента** для отслеживания злоупотреблений системой споров;
  - **живые фотографии продуктов**: помимо подготовленных студийных фотографий, для каждого товара выкладываются **актуальные «живые» фотографии** с прилавка/витрины:
    - фото делаются регулярно (ежедневно или при новых поступлениях);
    - указывается дата съёмки;
    - показывают реальное состояние товара, который будет доставлен;
    - повышают доверие клиента к качеству продукции;
  - логика с весом:
    - клиент заказывает, например, 500гр товара;
    - отмерить ровно 500гр сложно, поэтому магазин отмеряет приблизительно;
    - **допустимая погрешность (tolerance)**: для каждого товара продавец устанавливает допустимое отклонение (по умолчанию ±10%, т.е. 0.9-1.1);
    - **недовес**: если вес ниже минимально допустимого, разница возвращается клиенту в виде **баллов** (бонусов), которые можно использовать в следующих заказах;
    - **в пределах tolerance**: отклонения в допустимом диапазоне не компенсируются (например, заказ 500гр → доставка 480гр при tolerance ±10% — это норма);
    - **перевес**: если отмерили больше максимально допустимого, перевес идёт за счёт продавца (клиент не доплачивает).

Основная идея для магазинов (селлеров):
- **бесплатная инфраструктура для ведения бизнеса** — платформа не берет абонентскую плату, только % от онлайн-заказов;
- **комплексное решение для управления магазином**:
  - **складской учёт**: простая, но функциональная система управления остатками, приход/расход, инвентаризация, списания;
  - **управление сотрудниками**: найм, увольнение, прикрепление к магазинам, настройка ролей и доступов;
  - **управление сменами**: открытие/закрытие смен, отслеживание рабочего времени, производительности;
  - **витрина и каталог**: настройка ассортимента, цен, фото, описаний; управление видимостью товаров;
  - **представление в интернете**: автоматическая страница магазина на платформе, доступная всем клиентам;
  - **инструменты маркетинга**: 
    - акции и скидки;
    - промокоды;
    - управление рейтингом и отзывами;
    - статистика эффективности рекламных кампаний;
  - **аналитика работы**:
    - онлайн-заказы (конверсия, средний чек, популярные товары);
    - офлайн-продажи (если ведется учет через платформу);
    - финансовые показатели (выручка, комиссия, чистая прибыль);
    - операционные метрики (скорость сборки, время доставки, % отмен);
    - сравнение периодов, динамика роста;
- **прозрачные финансы**:
  - расчётные периоды с детализацией по каждому заказу;
  - автоматические отчёты и выплаты;
  - онлайн-доступ к балансу и транзакциям в любой момент;
- **минимальный порог входа**: не нужны большие инвестиции в IT-инфраструктуру, достаточно просто подключиться к платформе.

---

## 2. Основные участники и их функционал

### 2.1. Клиент (Customer)

**Сущность:** `Customer` с полями `telegramId` (unique), `phone` (unique)

**Авторизация (MVP):**
- **Регистрация:** через Telegram-бота (`@fruktgo_bot`)
  - Получение данных из Telegram (telegramId, firstName, username)
  - Запрос телефона через contactRequest (если не предоставлен)
  - Автоматическое создание Customer
- **Вход на веб-панель:** генерация одноразового кода (6 цифр, 5 минут)
  - Код генерируется на веб-панели по кнопке "Войти"
  - Код вводится в боте для подтверждения
  - Автоматический вход после подтверждения

**Основной функционал:**
- **Каталог:** просмотр товаров по магазинам, поиск, фильтрация по категориям/цене
- **Корзина:** добавление товаров, управление количеством (с учетом шага продажи), мердж гостевой корзины при логине
- **Заказ:** выбор адреса доставки/самовывоза, оплата онлайн (СБП/карта), отслеживание статуса
- **Профиль:** управление адресами доставки, контактами, историей заказов
- **Поддержка:** создание обращений по заказам (качество, недовоз), система споров, оценки/отзывы
- **Гарантия качества:** открытие споров при проблемах, возврат денег/скидки, накопление баллов за недовес

**Ключевые процессы:**
- Регистрация через Telegram → просмотр каталога → добавление в корзину → оплата → отслеживание → получение → оценка

---

### 2.2. Селлер (Seller)

**Сущность:** `Seller` с полями `telegramId`, `phone`, `email`, `inn` (ИНН), `companyName`

**Авторизация:**
- **Регистрация:** через отдельного Telegram-бота для селлеров (`@fruktgo_seller_bot`)
  - Получение данных из Telegram (telegramId, firstName, username)
  - Запрос телефона через contactRequest (если не предоставлен)
  - Пошаговый сбор данных: email, тип лица, ИНН, компания, ОГРН
  - Создание Seller со статусом `verifiedStatus = IS_CHECKING`
  - Верификация платформой (до 24 часов)
- **Вход на панель селлера:** генерация одноразового кода (аналогично Customer)
  - Код генерируется на `https://seller.fruktgo.com`
  - Код вводится в боте для подтверждения

**Основной функционал:**
- **Регистрация:** заполнение реквизитов (ИНН, ОГРН, компания), верификация платформой (`IS_CHECKING` → `VERIFIED`/`NOT_VERIFIED`)
- **Управление магазинами:** создание/настройка магазинов (адрес, режим работы, зона доставки), прикрепление товаров
- **Каталог товаров:**
  - Создание `Product` (название, категория, цена, единица измерения, шаг продажи)
  - Распределение по магазинам → создание `ShopProduct` (остатки, статус ACTIVE/PAUSED)
  - Управление остатками, ценами, изображениями по каждому магазину
- **Сотрудники:** найм, настройка ролей, прикрепление к магазинам, блокировка
- **Финансы:**
  - Просмотр расчётных периодов по магазинам (`SettlementPeriod`)
  - Аккумуляция денег в `SellerAccount`
  - Вывод средств
- **Аналитика:** заказы, выручка, популярные товары, эффективность магазинов

**Двухуровневая модель товаров:**
```
Seller
  └── Product (мастер-каталог)
       └── ShopProduct (распределение по магазинам)
            └── Order (заказы)
```

---

### 2.3. Магазин (Shop)

**Сущность:** `Shop` с полями `shopName`, `owner` (Seller), `status` (OPENED/CLOSED/PAUSED), `verifiedStatus`

**Контекст:**
- Конкретная торговая точка с адресом и зоной обслуживания
- **Все заказы привязаны к одному Shop** (нет мульти-магазинной корзины в MVP)
- Остатки и складской учёт ведутся по Shop
- Имеет собственный `ShopAccount` для расчётов

**Вход в панель управления магазином:**
- **Seller:** через бота селлера (`@fruktgo_seller_bot`)
  - Команда `/shops` → выбор магазина
  - Генерация одноразового кода для входа на `https://shop.fruktgo.com/manage`
  - Режим: владелец/менеджер (просмотр + настройки)
- **Employee:** через тот же бот селлера
  - Команда `/shop` → выбор магазина (из прикрепленных)
  - Генерация кода для входа на `https://shop.fruktgo.com`
  - Режим: оператор (выполнение задач)
  - Получение уведомлений в боте (заказы, задачи, смены)

**Функционал панели магазина:**
- **Заказы:** очередь заказов по статусам, принятие в работу, сборка, выдача/доставка
- **Склад:** приёмка товара, списания (порча, просрочка), инвентаризация
- **Смены:** открытие/закрытие смен сотрудников, контроль рабочего времени
- **Статус магазина:** открыт/закрыт для заказов, управление доступностью

---

### 2.4. Сотрудники магазина (Employee)

**Сущность:** `Employee` с полями `telegramId`, `phone`, `employer` (Seller), `pinnedTo` (Shop)

**Найм и авторизация:**
1. **Создание:** Seller создаёт сотрудника в панели (имя, телефон, роли, магазины)
2. **Приглашение:** генерируется ссылка + QR-код
   ```
   https://t.me/fruktgo_seller_bot?start=invite_abc123xyz
   ```
3. **Подтверждение:** сотрудник переходит по ссылке → открывается **бот селлера**
4. **Активация:** сотрудник принимает приглашение → привязка `telegramId` → статус `ACTIVE`
5. **Вход в панель:** команда `/shop` в боте → выбор магазина → генерация кода → вход на `https://shop.fruktgo.com`

**Роли:**
- Кассир, сборщик, упаковщик, курьер, кладовщик, менеджер смены

**Основной функционал:**
- **Смены (`Shift`):**
  - Открытие смены → работа → паузы → закрытие
  - State machine: OPEN → PAUSED → RESUME → CLOSING → CLOSED
  - SLA-снэпшот фиксируется при открытии
- **Задачи (`Task`):**
  - Сборка заказов (picking): взять в работу, отметить собранные позиции, завершить
  - Доставка: получить список заказов, отметить статус доставки
  - Склад: приёмка, списания, инвентаризация
- **Панель магазина:** выполнение операционных действий в рамках своих ролей
- **Уведомления в боте:**
  - Новый заказ: "📦 Новый заказ #1234 ждёт сборки"
  - Задача: "⏰ Пора собирать заказ #1234"
  - Смена: "🕐 Ваша смена начинается через 30 минут"
  - Проблемы: "⚠️ Заказ #1234 просрочен"

**Инвариант:** Любые операции магазина выполняются от имени активного `Employee`, привязанного к `Shop`.

---

### 2.5. Сотрудники платформы (PlatformStaff)

**Сущность:** `PlatformStaff` с полями `email`, `phone`, `roles`

**Роли (фундамент):**
- `platform_admin` — полный доступ (единственный в MVP)
- `platform_finance` — финансы, расчёты, выплаты (будущее)
- `platform_support` — поддержка клиентов/селлеров (будущее)
- `platform_seller_manager` — модерация селлеров (будущее)
- `platform_risk` — антифрод (будущее)

**Функционал (admin в MVP):**
- **Селлеры:** модерация заявок, верификация (`IS_CHECKING` → `VERIFIED`), блокировка
- **Магазины:** активация, блокировка, контроль доступности
- **Каталог:** модерация товаров, скрытие нарушений
- **Заказы:** просмотр всех заказов, решение проблемных кейсов, ручные корректировки
- **Финансы:**
  - Управление расчётными периодами (`SettlementPeriod`)
  - Ручные корректировки (штрафы, бонусы)
  - Запуск выплат (`Payout`)
- **Поддержка:** работа с обращениями (`SupportCase`), инициация возвратов
- **Аудит:** все критичные действия логируются в `AuditLog`

**Доступ:** Бэкофис `/api/platform/**`, проверка через `PlatformAuthGuard` + `PlatformRolesGuard`

---

### 2.6. Внешние сервисы

- **Платёжный провайдер** (ЮKassa): оплата клиентом, СБП/карта, возвраты
- **Служба доставки** (Dostavista): интеграция для курьеров (опционально)
- **Telegram Bot API**: авторизация, уведомления, боты для сотрудников

---

## 3. Базовая ценность и потоки денег

### 3.1. Что происходит в деньгах (в общем виде)

1. Клиент создаёт заказ и оплачивает его **онлайн** (СБП/карта).
2. Деньги поступают на сторону платформы / платёжного провайдера (точная схематика — агентская).
3. Платформа:
   - удерживает свою комиссию и возможные сборы;
   - фиксирует «доход селлера» по заказу;
   - в рамках расчётных периодов формирует выплаты селлерам (Payout).

### 3.2. Агентская модель (предполагаемая)

> **Важно:** здесь схема описана как целевая, детали могут меняться.

- Платформа выступает агентом:
  - принимает оплату от клиента;
  - реализует товары **от имени селлера** (агентский договор);
  - перечисляет селлеру выручку за вычетом комиссии и удержаний.
- Комиссия платформы (динамическая):
  - Базовая: % от суммы заказа (10–30%, зависит от договорённости);
  - Может снижаться при выполнении условий:
    - долгое сотрудничество (например, -2% после 6 месяцев);
    - высокий оборот (например, -1% при обороте >500к/мес);
    - высокий рейтинг качества;
  - Может повышаться как штраф:
    - много жалоб от покупателей (+3-5%);
    - систематические нарушения SLA;
    - низкий рейтинг магазина;
  - Возможна минимальная комиссия по заказу (например, 50₽ при сумме <500₽).

### 3.3. Расчётные периоды и выплаты

- Для каждого **магазина** (Shop) ведётся сущность `SettlementPeriod` (в модуле `finance/shop-account`):
  - период (даты);
  - валовой оборот по заказам магазина;
  - комиссия платформы (с учетом динамики);
  - возвраты/штрафы;
  - итог к выплате;
  - статус (open / closed / paid / frozen).
- После окончания периода и расчётов:
  - деньги, готовые к выводу, **аккумулируются** в `SellerAccount` (модуль `finance/seller-account`);
  - продавец видит общую сумму по всем своим магазинам;
- Выплаты (`Payout`) делаются **с задержкой** (например, раз в 2 недели, после закрытия периода).

Бекендовые инварианты (high level):
- Заказы входят в период по дате доставки/завершения (или по дате оплаты — надо будет зафиксировать).
- Закрытый период **не изменяется**; корректировки делаются отдельными транзакциями.
- Все действия по ручным корректировкам должны быть связаны с `PlatformStaff`.

---

## 4. Ключевые бизнес-сущности (на концептуальном уровне)

Здесь — не техническая схема БД, а «что есть что» с точки зрения бизнеса.

### 4.1. Каталог и товары

- `Product` — бизнес-единица товара (например, «Яблоко Гала»):
  - базовая информация, цена, категория;
  - **tolerance** (допустимая погрешность веса): коэффициент отклонения (по умолчанию 0.9-1.1);
  - студийные фотографии (подготовленные);
- `SKU` — конкретный SKU/вариант (упаковка, вес, размер).
- `ShopProduct` / `Price` — связь товара с магазином:
  - цена в конкретном магазине;
  - доступность;
  - **живые фотографии** (актуальные с прилавка) с датами съёмки;
  - настройки витрины (сортировка, коллекции).

### 4.2. Заказы и оплата

- `Order`:
  - клиент;
  - магазин (Shop);
  - список `OrderItem`;
  - суммы;
  - способ получения (доставка / самовывоз);
  - статусы (created / pending_payment / paid / picking / ready / in_delivery / delivered / canceled / refund и т.д.).
- `Payment`:
  - платёжный метод;
  - сумма;
  - статусы (init / success / failed / refunded).
- `Refund`:
  - ссылка на Order / Payment;
  - сумма (частичная/полная);
  - причина.

### 4.3. Склад и остатки

- `Inventory` / `Stock`:
  - остатки по SKU в рамках магазина.
- `StockMovement`:
  - тип (initial / intake / sale / return / write_off / inventory_adjustment);
  - количество (±);
  - ссылка на источник (поставка, заказ, инвентаризация).
- Инвариант:
  - текущий остаток по SKU в Shop = сумма всех движений по SKU в этом Shop.

### 4.4. Финансы

- `SettlementPeriod`:
  - селлер;
  - период;
  - суммы и статус.
- `SettlementTransaction`:
  - тип (доход по заказу, возврат, штраф, корректировка и т.п.);
  - ссылка на Order/Refund/ручное действие;
  - знак (+/-).
- `Payout`:
  - селлер;
  - связанный период;
  - сумма;
  - статус выплаты.

### 4.5. Операции магазина (операционный контур)

- `Task`:
  - тип (picking, packing, delivery, stock_intake, stock_write_off, inventory и т.д.);
  - статус (new / in_progress / completed / canceled);
  - ссылки на Order / Shop / Employee / Shift.
- `Shift`:
  - сотрудник;
  - магазин;
  - время начала/окончания;
  - статус (active / closed).

### 4.6. Поддержка и инциденты

- `SupportCase`:
  - инициатор (client / seller / employee / platform);
  - связанный Order / Shop / Seller;
  - статус (open / in_progress / resolved / closed);
  - история сообщений/решений.
- `AuditLog`:
  - кто (PlatformStaff / Seller / Employee);
  - что сделал;
  - над чем;
  - когда.

---

## 5. Основные бизнес-процессы

Детальное описание процессов см. в `/docs/business/processes/`:

- **Заказ** (`order-flow.md`): создание, оплата, сборка, доставка, завершение
- **Регистрация** (`registration-flow.md`): онбординг клиентов, селлеров, сотрудников
- **Оплата и финансы** (`payment-flow.md`): платежи, возвраты, расчётные периоды
- **Складской учёт** (`inventory-flow.md`): приёмка, списания, инвентаризация
- **Смены** (`shift-flow.md`): открытие/закрытие смен, задачи сотрудников
- **Поддержка** (`support-flow.md`): обращения, споры, возвраты

Краткая схема жизненного цикла заказа:
```
Клиент → Корзина → Оплата → [Shop] Сборка → Доставка → Получение → Оценка
                                ↓
                          SettlementPeriod → SellerAccount → Payout
```

---

## 6. Ключевые бизнес-инварианты (важно для бэкенда)

На уровне бизнес-логики, которые желательно держать как «правила вселенной»:

1. **Каждый заказ привязан к одному магазину (`Shop`).**  
   В MVP нет мульти-магазинной корзины.

2. **Остатки считаются через движения.**  
   Нет прямого ручного редактирования поля `quantity` без `StockMovement`.

3. **Оплаченный заказ не «удаляется».**  
   Он может:
   - переходить в статусы отмены/возврата,
   - корректироваться через `Refund` и финансовые транзакции.

4. **Закрытый расчётный период не переписывается.**  
   Любые изменения постфактум — только через дополнительные `SettlementTransaction`.

5. **Любое критичное действие PlatformStaff (финансы, блокировки, тарифы) — с аудитом.**

6. **Любая операционная активность магазина должна быть связана с `Employee` и `Shop`.**

---

## 7. MVP-ограничения (для фокуса бэка)

Чтобы бэкенд не «улетел» в overengineering, фиксируем примерные ограничения MVP (могут корректироваться):

- **поддержка нескольких городов**: платформа работает в множестве городов, каждый магазин привязан к своему городу, зоны доставки и цены настраиваются индивидуально;
- один тип платформенного сотрудника — `platform_admin`;
- нет сложной системы лояльности/реферальных схем (можно закладывать хук, но не реализовывать);
- один платёжный провайдер;
- одна модель доставки (или простая «свой курьер», интеграции позже);
- базовая аналитика (без отдельного BI-движка, но с возможностью агрегаций по заказам/селлерам/магазинам).

---

## 8. Как использовать этот документ при разработке бэка

- При проектировании новых модулей:
  - сверяться, на каком уровне эта логика — клиент/магазин/платформа;
  - проверять, не ломает ли фича инварианты (остатки, Settlements, связи с Shop/Seller).
- При общении с ИИ:
  - давать этот документ как основной контекст по бизнеску;
  - ссылаться на конкретные разделы (например: «см. раздел 4.2 про Order/Payment/Refund»).
- При уточнениях:
  - править в первую очередь этот файл, затем уже детальные `*-flows.md` и `domain-*.md`.

Документ — v0.1 и предполагает дальнейшую детализацию (особенно по финансам, складу и state machine заказов).
