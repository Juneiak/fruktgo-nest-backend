# Процесс: Регистрация и онбординг

**Участники:** Customer, Seller, Employee, PlatformStaff  
**Зависимости:** Telegram Bot API

---

## Краткое содержание

### Основная идея

Все пользователи регистрируются через **Telegram бота**: клиенты в `@fruktgo_bot`, продавцы и сотрудники в `@fruktgo_seller_bot`. Авторизация на веб-панелях через **6-значные коды** (генерируются на сайте, вводятся в боте).

**Три роли** с разными процессами регистрации: Customer (мгновенно), Seller (с верификацией ИНН), Employee (по приглашению от продавца).

### Ключевая логика

**Customer:**
- Открывает бот → автоматически получает данные из Telegram → сразу активен
- Без верификации, без одобрения — полностью автоматическая регистрация
- Вход на панель: генерация кода → ввод в боте → автовход

**Seller:**
- Регистрация через бот с пошаговым сбором: email, ИНН, тип лица, ОГРН
- Статус `IS_CHECKING` — заявка уходит на модерацию платформой
- PlatformStaff проверяет ИНН в ЕГРЮЛ → решение: VERIFIED / NOT_VERIFIED
- Только после верификации может создавать магазины

**Employee (сотрудник):**
- Создаётся продавцом с указанием ролей и магазинов
- Отправляется приглашение (ссылка в Telegram или SMS с QR-кодом)
- Сотрудник принимает приглашение в боте → статус ACTIVE
- Автоматически получает доступ к указанным магазинам

**Shop (магазин):**
- Создаётся верифицированным продавцом
- Указывается: город, адрес, зоны доставки, режим работы
- Проходит верификацию платформой (проверка адреса, документов)
- После одобрения может принимать заказы

---

## 1. Customer (клиенты)

**Бот:** `@fruktgo_bot`

### Регистрация

1. Открытие бота → приветствие + согласие на обработку данных
2. Автополучение из Telegram: `telegramId`, `firstName`, `username`
3. **Если телефон нет:** кнопка contactRequest "📞 Отправить номер"
4. Создание `Customer` → статус `ACTIVE`
5. Уведомление: "✅ Регистрация завершена! Панель: https://app.fruktgo.com/customer"

**API:** `POST /customer/register`

### Вход на веб-панель

1. На сайте кнопка "Войти через Telegram" → генерация кода (6 цифр, 5 минут)
2. Ввод кода в боте → привязка к `telegramId` → автовход

**API:**
- `POST /auth/web/generate-code`
- `POST /auth/web/confirm-code`

---

## 2. Seller (продавцы)

**Бот:** `@fruktgo_seller_bot`

### Регистрация

1. Открытие бота → приветствие + согласие
2. Получение из Telegram: `telegramId`, `firstName`, `username`
3. **Если телефон нет:** contactRequest
4. **Пошаговый сбор данных:**
   - Email
   - Тип лица (ИП/ООО/Самозанятый)
   - ИНН (10-12 цифр)
   - Название компании
   - ОГРН/ОГРНИП
5. Создание `Seller` → `verifiedStatus = IS_CHECKING`, `status = PENDING`
6. Уведомление: "✅ Заявка на проверке! Результат в течение 24 часов"

**API:** `POST /seller/register`

### Верификация

1. `PlatformStaff` в `/platform/sellers` проверяет ИНН в ЕГРЮЛ/ЕГРИП
2. Решение: `VERIFIED` (одобрено) / `NOT_VERIFIED` (отклонено) + причина
3. Уведомление в боте о результате

**API:** `PATCH /platform/sellers/:id/verify`

### Вход на панель

Аналогично Customer: генерация кода на `https://seller.fruktgo.com` → ввод в боте

**API:**
- `POST /auth/seller/web/generate-code`
- `POST /auth/seller/web/confirm-code`

**Правила:**
- Telegram + phone + email + ИНН уникальны
- Верификация обязательна для создания магазинов
- Один ИНН = один Seller

---

## 3. Shop (магазин)

**Входные условия:** Seller верифицирован

### Создание

1. Seller в панели → "Создать магазин"
2. Заполнение данных:
   - **Город** (выбор из списка поддерживаемых городов: Алматы, Астана и т.д.)
   - **Адрес** (улица, дом, корпус)
   - **Координаты** (определяются автоматически через геокодер)
   - **Зона доставки** (радиус или полигоны, настраиваются индивидуально для города)
   - **Режим работы** (часы работы по дням недели)
   - **SLA** (время принятия заказа, время сборки)
3. Создание `Shop`:
   ```typescript
   {
     shopName: "Фруктовая база",
     address: {
       city: "Алматы",
       street: "ул. Толе би, 45",
       coordinates: { lat: 43.238949, lng: 76.889709 }
     },
     deliveryZones: [
       { name: "Центр", basePrice: 300, polygon: [...] }
     ],
     status: "OPENED",
     verifiedStatus: "IS_CHECKING"
   }
   ```
4. Автосоздание `ShopAccount`
5. Верификация платформой → уведомление

**API:** 
- `POST /seller/shops`
- `PATCH /platform/shops/:id/verify`

### Вход в панель управления

**Seller (владелец):**
1. В боте `/shops` → выбор магазина → кнопка "Войти в панель"
2. Генерация кода → вход на `https://shop.fruktgo.com/manage`
3. Режим: просмотр + настройки (БЕЗ операционных действий)

**Employee (оператор):**
1. В боте `/shop` → выбор из прикрепленных магазинов
2. Генерация кода → вход на `https://shop.fruktgo.com`
3. Режим: выполнение задач (смены, сборка, доставка)

**API:**
- `POST /auth/seller/shop/generate-code`
- `POST /auth/employee/web/generate-code`

---

## 4. Employee (сотрудники)

**Бот:** `@fruktgo_seller_bot` (тот же, что у селлера)

### Найм

1. Seller создаёт сотрудника: имя, телефон, роли, магазины
2. Генерация ссылки + QR-код:
   ```
   https://t.me/fruktgo_seller_bot?start=invite_abc123xyz
   ```
3. Отправка: Telegram/SMS/QR лично

**API:** `POST /seller/employees`

### Подтверждение

1. Сотрудник переходит по ссылке/сканирует QR → открывается бот селлера
2. Бот показывает: компания, магазины, роли
3. Кнопки: "✅ Принять" / "❌ Отклонить"
4. **Если принял:**
   - Привязка `telegramId` → статус `ACTIVE`
   - Уведомление: "✅ Приглашение принято! Команда /shop для работы"
5. **Если отклонил:**
   - Статус `REJECTED` → уведомление селлеру

**API:**
- `GET /employee/invite/:token`
- `POST /employee/invite/:token/accept`
- `POST /employee/invite/:token/reject`

### Работа

1. Команда `/shop` → выбор магазина
2. Генерация кода для входа на `https://shop.fruktgo.com`
3. Выполнение операций: смены, сборка, доставка
4. **Уведомления в боте:**
   - "📦 Новый заказ #1234"
   - "⏰ Пора собирать"
   - "🕐 Смена через 30 минут"

**API:** `POST /auth/employee/web/generate-code`

**Правила:**
- Приглашение действительно 7 дней
- Телефон уникален в рамках селлера
- Сотрудник может быть в нескольких магазинах одного селлера

---

## Техническая сводка

### Сущности и статусы

| Сущность | Статусы | Уникальные поля |
|----------|---------|-----------------|
| `Customer` | ACTIVE, BLOCKED | `telegramId`, `phone` |
| `Seller` | PENDING, ACTIVE, REJECTED | `telegramId`, `phone`, `email`, `inn` |
| `Shop` | OPENED, CLOSED, PAUSED | - |
| `Employee` | PENDING, ACTIVE, REJECTED, BLOCKED | `phone` (в рамках селлера) |

**Верификация:**
- `IS_CHECKING` - на проверке
- `VERIFIED` - одобрен
- `NOT_VERIFIED` - отклонён

### API

**Customer:**
- `POST /customer/register`
- `POST /auth/web/generate-code`
- `POST /auth/web/confirm-code`

**Seller:**
- `POST /seller/register`
- `POST /seller/shops`
- `POST /seller/employees`
- `POST /auth/seller/web/generate-code`
- `POST /auth/seller/web/confirm-code`

**Platform:**
- `PATCH /platform/sellers/:id/verify`
- `PATCH /platform/shops/:id/verify`

**Employee:**
- `GET /employee/invite/:token`
- `POST /employee/invite/:token/accept`
- `POST /auth/employee/web/generate-code`

---

> **Статус:** ✅ Готов  
> **Обновлено:** 2024-11-23