# Процесс: Верификация и блокировка

**Участники:** Customer, Seller, Shop, Employee, PlatformStaff  
**Зависимости:** Seller Module, Shop Module, Customer Module

---

## Краткое содержание

### Основная идея

Продавцы и магазины проходят **верификацию** платформой перед началом работы. Продавец предоставляет ИНН, платформа проверяет в ЕГРЮЛ/ЕГРИП. Магазин проверяется по адресу и документам. За нарушения правил пользователи могут быть **заблокированы** (временно или навсегда).

**Схема:** Регистрация → проверка данных → решение (VERIFIED / NOT_VERIFIED / BLOCKED).

### Ключевая логика

**Верификация Seller:**
- При регистрации статус `IS_CHECKING`
- PlatformStaff проверяет ИНН в ЕГРЮЛ/ЕГРИП
- Решение: `VERIFIED` (может создавать магазины) или `NOT_VERIFIED` (причина отклонения)
- Время проверки: до 24 часов

**Верификация Shop:**
- Создаётся только верифицированным продавцом
- Статус `IS_CHECKING` при создании
- Проверяется: адрес, соответствие документам, реальность точки
- После одобрения может принимать заказы

**Блокировка:**
- Customer — за злоупотребление спорами, мошенничество
- Seller — блокирует все магазины продавца
- Shop — блокирует конкретный магазин
- Типы: `TEMPORARY` (с указанием срока) или `PERMANENT`

**Разблокировка:**
- Пользователь может подать апелляцию
- PlatformStaff рассматривает причины блокировки
- Решение: разблокировать или отклонить апелляцию

### Причины блокировки

Частые споры, плохое качество товаров, нарушение сроков доставки, мошенничество, подделка документов, массовые жалобы клиентов.

---

## Обзор

Процессы проверки пользователей и магазинов, а также система блокировок за нарушения.

**Верификация:**
- **Seller** - обязательная проверка ИНН перед созданием магазинов
- **Shop** - проверка магазина перед началом работы
- **Customer** - опциональная верификация (MVP: автоматически ACTIVE)
- **Employee** - подтверждение приглашения (не верификация)

**Блокировка:**
- **Customer** - за нарушения правил платформы
- **Seller** - за нарушения, блокирует все магазины
- **Shop** - за нарушения конкретного магазина
- **Employee** - работодателем или платформой

---

## 1. Верификация Seller

**Актор:** Seller, PlatformStaff

### Сценарий

1. **Регистрация** (через Telegram-бот):
   - Seller заполняет данные: ИНН, название компании, email
   - Система создаёт `Seller`:
     ```typescript
     {
       inn: "1234567890",
       companyName: "ООО Фруктовая база",
       verifiedStatus: "IS_CHECKING",  // На проверке
       status: "PENDING"                // Не активен
     }
     ```
   - Уведомление в бот: "✅ Заявка отправлена! Результат в течение 24 часов"

2. **Проверка PlatformStaff:**
   - Админ в `/platform/sellers` → фильтр `verifiedStatus = IS_CHECKING`
   - Проверяет:
     - ИНН в ЕГРЮЛ/ЕГРИП
     - Соответствие данных
     - Отсутствие в чёрных списках
   - Принимает решение:
     - **Одобрить:** `VERIFIED` + `ACTIVE`
     - **Отклонить:** `NOT_VERIFIED` + причина

3. **Уведомление:**
   - **Одобрено:**
     ```
     🎉 Заявка одобрена!
     Теперь вы можете создавать магазины.
     Панель: https://seller.fruktgo.com
     ```
   - **Отклонено:**
     ```
     ❌ Заявка отклонена
     Причина: {причина от модератора}
     Вы можете подать заявку повторно с корректными данными.
     ```

**API:**
- `POST /seller/register` - регистрация
- `PATCH /platform/sellers/:id/verify` - решение админа

### Статусы

| VerifiedStatus | Описание | Действия доступны |
|----------------|----------|-------------------|
| `IS_CHECKING` | На проверке | Нет, ожидание |
| `VERIFIED` | Верифицирован | ✅ Создание магазинов |
| `NOT_VERIFIED` | Отклонён | ❌ Создание запрещено |

**Правила:**
- Только `VERIFIED` селлеры могут создавать магазины
- Один ИНН = один Seller (уникальность)
- Повторная заявка возможна с новыми данными

---

## 2. Верификация Shop

**Актор:** Seller, PlatformStaff

### Сценарий

1. **Создание магазина:**
   - Seller в панели → "Создать магазин"
   - Входное условие: `seller.verifiedStatus = VERIFIED`
   - Заполнение: адрес, зона доставки, режим работы, SLA
   - Система создаёт `Shop`:
     ```typescript
     {
       shopName: "Фруктовая база на Ленина",
       address: {...},
       status: "OPENED",
       verifiedStatus: "IS_CHECKING"  // На проверке
     }
     ```
   - Автосоздание `ShopAccount`

2. **Проверка PlatformStaff:**
   - Админ проверяет:
     - Корректность адреса
     - Зона обслуживания
     - SLA параметры адекватны
   - Решение:
     - **Одобрить:** `VERIFIED`
     - **Отклонить:** `NOT_VERIFIED` + причина

3. **Уведомление в бот селлера**

**API:**
- `POST /seller/shops` - создание
- `PATCH /platform/shops/:id/verify` - решение админа

### Статусы

Аналогично Seller: `IS_CHECKING` → `VERIFIED` / `NOT_VERIFIED`

**Правила:**
- Магазин может работать в статусе `IS_CHECKING` (мягкая верификация)
- `NOT_VERIFIED` → магазин скрывается из публичного каталога

---

## 3. Верификация Customer

**MVP:** Автоматическая активация без верификации

### Текущая логика

1. Customer регистрируется через Telegram
2. Система создаёт с `verifiedStatus = IS_CHECKING`
3. **Автоматически:** `status = ACTIVE` (может делать заказы)

### Будущая верификация (backlog)

- Подтверждение телефона через SMS
- Подтверждение email
- Документы для крупных заказов (>определённой суммы)

**Правила:**
- В MVP все клиенты активны сразу
- `verifiedStatus` зарезервирован для будущего

---

## 4. Блокировка Customer

**Актор:** PlatformStaff

### Причины блокировки

- Множественные отмены заказов
- Мошенничество
- Нарушение правил платформы
- Спам в обращениях

### Сценарий

1. Админ в `/platform/customers/:id` → "Заблокировать"
2. Заполнение:
   - **Тип блокировки:**
     - `BLOCKED` - постоянная блокировка
     - `SUSPENDED` - временная (требует `blockedUntil`)
   - **Причина:** текст для пользователя
   - **Код:** внутренний код нарушения (FRAUD, SPAM, ABUSE, etc.)
3. Система обновляет:
   ```typescript
   {
     blocked: {
       status: "BLOCKED",
       reason: "Множественные попытки мошенничества",
       code: "FRAUD_DETECTED",
       by: adminId,
       blockedAt: new Date(),
       blockedUntil: null  // Бессрочно
     }
   }
   ```
4. **Эффекты:**
   - Клиент не может создавать заказы
   - Активные заказы продолжают выполняться
   - Вход в систему возможен (только чтение)

**API:** `PATCH /platform/customers/:id/block`

### Разблокировка

```typescript
{
  blocked: {
    status: "ACTIVE",
    reason: null,
    code: null,
    // Остальные поля очищаются
  }
}
```

---

## 5. Блокировка Seller

**Актор:** PlatformStaff

### Причины блокировки

- Нарушение договора
- Систематические жалобы
- Проблемы с качеством товаров
- Невыполнение SLA

### Сценарий

1. Админ блокирует Seller
2. **Каскадная блокировка:**
   - Блокируются **все магазины** Seller
   - Закрываются **все смены** в магазинах
   - Отменяются **активные заказы**
   - Сотрудники теряют доступ к работе
3. Уведомление селлеру в Telegram

**API:** `PATCH /platform/sellers/:id/block`

**Правила:**
- Блокировка Seller → автоблокировка всех Shop
- Финансовые операции замораживаются
- Возможна разблокировка после решения проблем

---

## 6. Блокировка Shop

**Актор:** PlatformStaff или Seller

### Сценарии блокировки

**Platform:**
- Нарушения конкретным магазином
- Проблемы с документами магазина
- Жалобы на конкретную точку

**Seller (мягкая блокировка):**
- Временная приостановка работы
- Ремонт, инвентаризация
- Отпуск

### Сценарий (Platform)

1. Админ блокирует Shop
2. **Эффекты:**
   - Закрывается текущая смена
   - Отменяются активные заказы
   - `status = CLOSED`
   - Магазин скрывается из публичного каталога
3. Уведомление селлеру

**API:** `PATCH /platform/shops/:id/block`

### Seller может сам приостановить

```typescript
// Через панель управления магазином
PATCH /seller/shops/:id
{
  status: "PAUSED"  // Временная пауза, не блокировка
}
```

**Отличие:**
- `PAUSED` (Seller) - временно, селлер контролирует
- `BLOCKED` (Platform) - платформа заблокировала

---

## 7. Блокировка Employee

**Актор:** Seller или PlatformStaff

### Сценарии

**Seller (работодатель):**
- Увольнение
- Дисциплинарные взыскания
- Окончание контракта

**Platform:**
- Мошенничество со стороны сотрудника
- Серьёзные нарушения

### Сценарий

1. Seller в панели → "Сотрудники" → "Заблокировать"
2. Система обновляет:
   ```typescript
   {
     blocked: {
       status: "BLOCKED",
       reason: "Увольнение по решению работодателя",
       by: sellerId
     }
   }
   ```
3. **Эффекты:**
   - Сотрудник не может открывать смены
   - Текущая смена закрывается
   - Доступ к панели магазина блокируется

**API:** `PATCH /seller/employees/:id/block`

---

## Техническая сводка

### Статусы блокировки (Blocked)

```typescript
interface Blocked {
  status: BlockStatus,     // ACTIVE/BLOCKED/SUSPENDED
  reason?: string,         // Причина для пользователя
  code?: string,           // Внутренний код
  by?: ObjectId,           // Кто заблокировал
  blockedAt?: Date,        // Когда
  blockedUntil?: Date      // До когда (для SUSPENDED)
}
```

| BlockStatus | Описание | Срок |
|-------------|----------|------|
| `ACTIVE` | Нет блокировки | - |
| `BLOCKED` | Постоянная блокировка | Бессрочно |
| `SUSPENDED` | Временная приостановка | До `blockedUntil` |

### Статусы верификации (VerifiedStatus)

| Статус | Описание | Применимо к |
|--------|----------|-------------|
| `IS_CHECKING` | На проверке | Seller, Shop |
| `VERIFIED` | Верифицирован | Seller, Shop, Customer |
| `NOT_VERIFIED` | Отклонён | Seller, Shop |

### API

**Верификация:**
- `PATCH /platform/sellers/:id/verify` - Seller
- `PATCH /platform/shops/:id/verify` - Shop

**Блокировка:**
- `PATCH /platform/customers/:id/block` - Customer
- `PATCH /platform/sellers/:id/block` - Seller (каскадно все Shop)
- `PATCH /platform/shops/:id/block` - Shop
- `PATCH /seller/employees/:id/block` - Employee (работодатель)
- `PATCH /platform/employees/:id/block` - Employee (платформа)

### Бизнес-правила

1. **Seller → Shop:** Только `VERIFIED` селлеры создают магазины
2. **Каскад блокировки:** Seller → все Shop → все Shift → отмена Orders
3. **SUSPENDED:** Требует `blockedUntil`, автоматическая разблокировка по истечении
4. **Верификация необратима:** `VERIFIED` → нельзя вернуть в `IS_CHECKING`
5. **Повторная заявка:** `NOT_VERIFIED` → можно подать снова с новыми данными

---

## Примеры

### Одобрение Seller

```typescript
// Админ одобряет селлера
await sellerPort.updateSeller(sellerId, {
  verifiedStatus: VerifiedStatus.VERIFIED,
  status: SellerStatus.ACTIVE
});

// Seller получает уведомление в Telegram
// Теперь может создавать магазины
```

### Блокировка Customer

```typescript
// Постоянная блокировка
await customerPort.blockCustomer(customerId, {
  status: BlockStatus.BLOCKED,
  reason: "Множественные попытки мошенничества",
  code: "FRAUD_DETECTED"
});

// Временная приостановка на 7 дней
await customerPort.blockCustomer(customerId, {
  status: BlockStatus.SUSPENDED,
  reason: "Нарушение правил платформы",
  code: "POLICY_VIOLATION",
  blockedUntil: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)
});
```

### Каскадная блокировка Seller

```typescript
// Блокируем селлера
await sellerPort.blockSeller(sellerId, {
  status: BlockStatus.BLOCKED,
  reason: "Систематические нарушения договора"
});

// Автоматически:
// 1. Все магазины селлера → BLOCKED
// 2. Все смены → CLOSED
// 3. Активные заказы → CANCELLED
// 4. Сотрудники теряют доступ
```

---

## Уведомления

### Верификация

**Seller одобрен:**
```
🎉 Ваша заявка одобрена!
Теперь вы можете создавать магазины и добавлять товары.
Панель: https://seller.fruktgo.com
```

**Seller отклонён:**
```
❌ Заявка отклонена
Причина: Некорректный ИНН или данные не соответствуют ЕГРЮЛ
Вы можете подать заявку повторно с корректными данными.
```

### Блокировка

**Customer заблокирован:**
```
⚠️ Ваш аккаунт заблокирован
Причина: Множественные нарушения правил платформы
Для разблокировки обратитесь в поддержку.
```

**Seller заблокирован:**
```
⛔ Ваш аккаунт приостановлен
Причина: Систематические жалобы на качество товаров
Все магазины временно закрыты.
Свяжитесь с поддержкой для решения вопроса.
```

---

> **Статус:** ✅ Готов  
> **Обновлено:** 2024-11-23